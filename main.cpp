#include "wireless_network.h"
#include "packet.h"
#include "buffer.h"
#include "protocol.h"
#include "transmitter.h"
#include "receiver.h"
#include "channel.h"
#include <iostream>
#include <vector>

using std::vector;
using std::cout;

int main()
{
	new Protocol = Protocol(1, 2, 3, 4);
	new Receiver = Receiver;
	new Transmitter = Transmitter(1);
	new WirelessNetwork = WirelessNetwork;
	new Packet = Packet(0);
	new Channel = Channel(1);
	new Buffer = Buffer;

	return 0;
}

